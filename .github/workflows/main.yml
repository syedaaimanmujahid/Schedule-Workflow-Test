name: Schedule Workflow Test

on: 
  schedule:
    - cron: "53 19 * * *"
      workflow-dispetch

jobs:
  interview:
    runs-on: ubuntu-latest

    steps:
      - name: Schedule Test
        run: echo "I've done Test"
